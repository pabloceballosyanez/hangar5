name: Deploy to Render

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render deploy
        run: |
          curl -sX POST "${{ secrets.RENDER_DEPLOY_HOOK || 'https://api.render.com/deploy/srv-d841qk3bc2fs73dn18og?key=4FcWICOJJCM' }}"
