name: Deploy to Render

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render deploy
        run: |
          curl -sX POST "${{ secrets.RENDER_DEPLOY_HOOK || 'https://api.render.com/deploy/srv-d8419d0g4nts73eo2ql0?key=MskkXHG_STw' }}"
